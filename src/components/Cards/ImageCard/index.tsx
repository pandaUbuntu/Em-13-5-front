import './style.css'

type SectionProps = {    
    title: string,
    children?: React.ReactNode
}

const ImageCard = () =>{

  return (
        <div>
          
        </div>
  )
}

export default ImageCard
